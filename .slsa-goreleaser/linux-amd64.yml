version: 1

env:
  - CGO_ENABLED=0

binary: semver-explode-linux-amd64
dir: ./cmd/semver-explode

goos: linux
goarch: amd64

flags:
  - -trimpath

ldflags:
  - "-s -w"
  - "-X main.Version={{ .Env.VERSION }}"
