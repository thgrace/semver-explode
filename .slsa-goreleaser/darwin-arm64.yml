version: 1

env:
  - CGO_ENABLED=0

binary: semver-explode-darwin-arm64
dir: ./cmd/semver-explode

goos: darwin
goarch: arm64

flags:
  - -trimpath

ldflags:
  - "-s -w"
  - "-X main.Version={{ .Env.VERSION }}"
